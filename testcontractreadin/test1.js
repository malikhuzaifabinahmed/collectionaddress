const fs = require("fs");
const { abi } = require("./abi");

// abi = fs.readFileSync("./blockdata.json", "utf8");
// pabi = JSON.parse(abi);
while (true) {
  while (true) {
    console.log("2nd loop");
    return;
  }
}
console.log(abi[0]);
